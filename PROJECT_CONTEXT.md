# Project Context

> **Auto-generated by [code-mapper](https://github.com/your-org/code-mapper).**  
> Read this file instead of scanning source files — it captures the full structure at a fraction of the token cost.
> `data_pipeline` · 1 files (python×1) · 0 classes · 4 functions · ~273 source lines

---
## File Structure

```
data_pipeline/
└── data_ingestion.py
```

## Class Diagram

_No classes found._

## Module Dependencies

_No internal dependencies detected._

## Symbol Index


**`data_ingestion.py`** `python`
- `def fetch_yfinance_aggs(ticker: str, interval: str, start_date: str, end_date: str) → DataFrame`  — Fallback: fetch OHLCV data via yfinance when Polygon key is unavailable.
- `def fetch_polygon_aggs(ticker: str, multiplier: int, timespan: str, start_date: str) → DataFrame`  — Fetch aggregates from Polygon.io.
- `def update_ticker_data(ticker: str, interval: str, force_full: bool)`  — Update parquet cache incrementally for a given ticker and interval.
- `def main()`